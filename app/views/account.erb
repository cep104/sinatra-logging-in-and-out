<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>title</title>
    <link rel="stylesheet" href="stylesheets/bootstrap.css" type="text/css">  
  </head>
  <body id="page-top">
    <%if Helpers.is_logged_in?(session)%><br>
    <h1>Welcome <%= Helpers.current_user(session).username%></h1><br>
    <h2>Your Balance <%= Helpers.current_user(session).balance %></h2><br>
  <h3><a href="/logout">Log Out</a></h3>
  </body>
</html>